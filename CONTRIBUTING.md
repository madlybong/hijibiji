# Contributing to GMDesigner

We use conventional commits and PRs.

## Development setup
- Install Bun.
- Run `bun install` at the root.
- Run `bun run dev` to start the local environment.

## Monorepo
We use Bun Workspaces. All apps are in `apps/` and shared libraries in `packages/`.
